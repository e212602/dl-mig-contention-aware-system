apiVersion: batch/v1
kind: Job
metadata:
  name: vllm-server-bloom-1b7
  labels:
    app.kubernetes.io/name: vllm-server-bloom-1b7
spec:
  parallelism: 1
  template:
    spec:
      containers:
      - name: vllm-server-bloom-1b7
        # command: ["/bin/bash"]
        args: 
        - "--model"
        - "$(MODEL_NAME)"
        - "--tensor-parallel-size"
        - "1"
        - "--max-model-len"
        - "4096"
        - "--async-scheduling"
        - "--limit-mm-per-prompt.video"
        - "0"   
        - "--no-enable-prefix-caching"
        # - "--cpu-offload-gb" 
        # - "0"
        # - "--kv-offloading-size"
        # - "0"
        - "--gpu_memory_utilization"
        - "0.9"
        env:
        - name: MODEL_NAME
          value: "bigscience/bloom-1b7"
        - name: HF_TOKEN
          valueFrom:
            secretKeyRef:
              name: hf-token-secret
              key: HF_TOKEN
        - name: HF_HOME
          value: "/root/.cache/huggingface/"
        image: docker.io/vllm/vllm-openai:latest
        imagePullPolicy: Never
        resources:
          requests:
            cpu: '10'
            memory: 8Gi
            nvidia.com/gpu: 1
          limits:
            cpu: '10'
            memory: 8Gi
            nvidia.com/gpu: 1
        stdin: true
        tty: true
        volumeMounts:
        - mountPath: /root/.cache/huggingface
          name: hf-cache
        - mountPath: /dev/shm
          name: dshm
        ports:
          - containerPort: 8000
            name: bloom-1b7
      restartPolicy: Never
      volumes:
      - name: hf-cache
        hostPath:
          path: /home/ubuntu/.cache/huggingface
          type: Directory
      - name: dshm
        emptyDir:
          medium: Memory
          sizeLimit: 16Gi   # see below
---
apiVersion: v1
kind: Service
metadata:
  name: vllm-service-bloom-1b7
spec:
  selector:
    job-name: vllm-server-bloom-1b7
  ports:
  - name: bloom-1b7
    protocol: TCP
    port: 8000
    targetPort: bloom-1b7
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: vllm-ingress
  annotations:
    # Specifies HAProxy Ingress as the controller
    haproxy-ingress.github.io/ssl-redirect: "false"
    # Optional: Increase timeout for long-running vLLM generation requests (in seconds)
    haproxy-ingress.github.io/timeout-connect: "15s"
    haproxy-ingress.github.io/timeout-server: "600s"
    haproxy-ingress.github.io/timeout-client: "600s"
spec:
  ingressClassName: haproxy
  rules:
  - host: localhost # Replace with your domain or IP host
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: vllm-service-bloom-1b7
            port:
              number: 8000